language: python
sudo: false

env:
  - TOXENV=py32
  - TOXENV=py33
  - TOXENV=py34
  - TOXENV=pypy3

install:
  - pip install --upgrade pip
  - pip --version
  - travis_retry pip install tox

script:
  - travis_retry tox
